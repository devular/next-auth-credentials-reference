{
  "name": "next-auth-credentials-reference",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "cypress": "NODE_ENV=test DATABASE_URL=\"file:./test.db\" npm run test:teardown && npm run test:setup && cypress open",
    "cypress:headless": "NODE_ENV=test cypress run",
    "e2e": "start-server-and-test dev http://localhost:3000 cypress",
    "test:setup": "DATABASE_URL=\"file:./test.db\" node scripts/test-setup.js",
    "test:teardown": "DATABASE_URL=\"file:./test.db\" node scripts/test-teardown.js",
    "e2e:headless": "NODE_ENV=test DATABASE_URL=\"file:./test.db\" npm run test:setup && next build && start-server-and-test start http://localhost:3000 cypress:headless && npm run test:teardown"
  },
  "dependencies": {
    "@hookform/resolvers": "^2.9.8",
    "@next-auth/prisma-adapter": "^1.0.4",
    "@prisma/client": "^4.3.1",
    "argon2": "^0.29.1",
    "next": "12.3.1",
    "next-auth": "^4.12.0",
    "react": "18.2.0",
    "react-dom": "18.2.0",
    "react-hook-form": "^7.36.1",
    "zod": "^3.19.1"
  },
  "devDependencies": {
    "@types/node": "18.7.23",
    "@types/react": "18.0.21",
    "@types/react-dom": "18.0.6",
    "cypress": "^10.9.0",
    "eslint": "8.24.0",
    "eslint-config-next": "12.3.1",
    "prisma": "^4.3.1",
    "start-server-and-test": "^1.14.0",
    "typescript": "4.8.3"
  }
}
